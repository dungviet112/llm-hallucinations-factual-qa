# Early Detection of Hallucinations in LLMs

## Overview
This repository implements the investigation for hallucination detection by treating the problem as binary classification over model artifacts, showing that internal representations can reveal whether an answer is likely to be factual before generation is fully complete.

There are 2 kinds of datasets to be used for experiments based on the length of generated text:
- Short-text response: evalutate text generated by an LLM ranging in the length from a few to several dozen tokens.
- Long-text response: evalutate text generated by LLM with the range of hundreds to thousands of tokens.

## Setup

Set up the conda env by downloading dependencies with **Python 3.10**
```sh
pip install -r requirements.txt
```

Clone the template configuration file `.env.example` to create your local environment file `.env` then add the Hugging Face access token to open-source LLMs in that file.

## Data sources

There are 2 datasets: **TriviaQA** (short-text response) and **FAVA-Annotation** (long-text response). The datasets will be saved in `./data/` folder after downloading.

**TriviaQA** consists of a set of numerous trivia questions, answers and evidence tuples, but here we only use randomly 10000 questions from the no-context setting where the model is only provided with questions without any supporting information. Normally, the response for each TriviaQA question just has around **30-50 tokens**.
```sh
# TriviaQA (no-context) sample format
{
    "question": question
    "answer": list of keywords
}
```
**FAVA-Annotation** contains 460 human-annotated responses, each response normally includes **500-700 tokens** and is accompanied by a label (hallucination/non-hallucination). These responses were generated by feeding knowledge-intensive prompts into models such as ChatGPT and Llama-2-Chat.

<!-- **TREX** consists of relationship triplets containing pairs of entities and their relationships, there are three different relationship categories: Capitals, Founders and Places of Birth. -->

## Artifact data collection progress

Artifact data collection is done in **result_collector.py** using **TriviaQA** dataset directly. According to the original method from the paper, for every sample, we gather some information including Q-A pair from dataset, response, artifacts of model, and label (hallucination/non-hallucination) then save them in pickle file. The artifacts consist of:
- **Softmax probablities**: The final distribution at the output layer that shows how confident the model is about the next specific word it is going to type
- **Feature attributions**: Scores showing how much each token in the input prompt influenced the generated output.
- **Self-attention weights**: Matrices that dictate how strongly the current token being generated is looking at or connecting to all previously generated tokens and the input context.
- **FC layer activations**: The vectors representing the neural activity inside the model deep layers, specifically the fully connected layer before predicting the next word.

The extension introduced in this project extracts the **contextual embeddings** of the generated response from the model hidden states. For a response consisting of multiple tokens, the hidden representation of each token can be extracted from a selected model layer: $h_1, h_2, ..., h_T$ where each $h_i$ is the contextual representation of token $i$. These token level representations can then be aggregated by mean pooling. The final vector provides a compact representation of the response in the latent space and is treated as other model artifacts.

Models/tokenizers are called from Huggingface. Softmax and contextual embeddings are collected directly from the model, attributions are collected using the integrated gradients (IG) method available in Captum and activations and attentions (model internal states) are collected using the **register_forward_hook** functionality.
```sh
python result_collector.py
```

<!-- ## Artifact data collection with pyvene

Artifacts includes activations, attention, softmax output, attributions.
Artifact data collection is done in **result_collector_pyvene.py** then is written in picke files.

Models/tokenizers are called from Huggingface. Softmax/logits are collected directly from the model, attributions are collected using the 
integrated gradients (IG) method available in Captum and activations and attentions are collected using the **pyvene** package which helps reduce the complexity of the code when collecting model internal states.
```sh
python result_collector_pyvene.py
``` -->

<!-- The experiment in **test_pyvene.ipynb** is about collecting artifacts from a sample QA after integrating with pyvene (the target is to check the type, shape of output) -->

## Classifiers

Training classifiers on IG, softmax, attention scores, FC activations, contextual embeddings across the models/datasets. **model.py** consists of several different classifier architectures (RNN, Single layer MLP, multi-layer MLP + Residual block, and Multi-layer Transformer + Residual block) for the artifacts, then train and test them to get AUROC score with **eval_classifier.py** on the data collected by **result_collector.py**. The evaluated result (AUROC score) then be saved in **score_result.txt**.
```sh
python eval_classifier.py
```

## Results

Overall, both AUROC score and Accuracy on classifier with Contextual Embeddings got better performance compared to other artifacts in the settting of short-text answer. However, in the context of long-text response, the improvement was only slight, sometime even lower than other features because the hallucination signal was distributed across the whole sentence instead of one spot in the short text case.


### Table result on TriviaQA
| Model | Classifier<br> | Feature<br> | AUROC<br> | Accuracy<br> | 
| ------| -------------- | ----------- | --------- | ------------ |
| Open-Llama-7B | RNN    | Feature attributions    | 0.62 | 0.63 |
|  | Single MLP     | Softmax probabilities        | 0.69 | 0.61 |
|  |      | Self-attention weights (layer 21)        | 0.74 | 0.70 |
|  |      | FC activations (layer 21)        | 0.73 | 0.69 |
|  |      | Contextual Embeddings (layer 32) | **0.86** | **0.78** |
|  | 2-layer MLP + Residual  | Softmax probabilities        | 0.70 | 0.67 |
|  |      | Self-attention weights (layer 19)        | 0.73 | 0.69 |
|  |      | FC activations (layer 21)        | 0.75 | 0.69 |
|  |      | Contextual Embeddings (layer 32) | **0.86** | **0.78** |
| Qwen2.5-7B-Instruct | RNN    | Feature attributions    | 0.53 | 0.64 |
|  | Single MLP     | Softmax probabilities | 0.63 | 0.61 |
|  |      | Self-attention weights (layer 21)        | 0.80 | 0.73 |
|  |      | FC activations (layer 22)        | 0.79 | 0.72 |
|  |      | Contextual Embeddings (layer 32) | **0.82** | **0.75** |
| Llama-3.1-8B-Instruct | RNN    | Feature attributions    | 0.55 | 0.69 |
|  | Single MLP     | Softmax probabilities | 0.65 | 0.71 |
|  |      | Self-attention weights (layer 16)        | 0.78 | 0.76 |
|  |      | FC activations (layer 17)        | 0.78 | 0.76 |
|  |      | Contextual Embeddings (layer 32) | **0.80** | **0.77** |

### Table result on FAVA-Annotation
| Model | Classifier<br> | Feature<br> | AUROC<br> | Accuracy<br> | 
| ------| -------------- | ----------- | --------- | ------------ |
| Vicuna-7B |  [LLM-Check](https://proceedings.neurips.cc/paper_files/paper/2024/file/3c1e1fdf305195cd620c118aaa9717ad-Paper-Conference.pdf)    | [Attention map score](https://proceedings.neurips.cc/paper_files/paper/2024/file/3c1e1fdf305195cd620c118aaa9717ad-Paper-Conference.pdf) (layer 19) | **0.71** | 0.66 |
|  | Single MLP             | Contextual Embeddings (layer 32)  | 0.67 | 0.66 |
|  | 2-layer MLP + Residual | Contextual Embeddings (layer 32)  | 0.69 | **0.68** |
| Llama-3-8B-Instruct | LLM-Check | Attention map score (layer 23) | 0.68 | 0.65 |
|  | Single MLP             | Contextual Embeddings (layer 32) | 0.68 | 0.66 |
|  | 2-layer MLP + Residual | Contextual Embeddings (layer 32) | **0.71** | **0.68** |

<!-- **Note**: The best performances are belonging to activations and attentions at last layer. -->

<!-- ## Plots

Data analysis (the plots in the paper) is done in **plots_tsne.ipynb** and **plots_entropy_and_pca.ipynb**. It corresponds to the 5.1 **Qualitative analysis** section of the paper, however most plots are collected in the appendix.

Once data is collected, we are iterested in comparative plots of softmax/IG attributions/activations across the models and datasets.
This is the reason why we collect the large dicts at the beginning of both notebooks. This is also a time consuming process, but note
that the notebook(s) can also be used on one model/dataset for fast experimentation.
Example: the data source directoiry (in our case **results**) would contain only capitals/falcon-40b_capitals_7_18.pickle while **founders**, **trivia**, **place_of_birth** stay empty. -->

## SelfCheckGPT

In this repo, selfcheckgpt is the baseline and compared to proposed method; a notebook is included. SelfcheckGPT does not perform well as the classifier, we hypothesize that this is because the models we use are small and the output for nonzero temperature is often subpar.
Selfcheckgpt uses the **bert-score** and **n-gram** methods from the its paper in **self_check_gpt.py**.
```sh
python self_check_gpt.py
```

---

## Repository Structure

```text
.
├── data/                              # Dataset directory (populated at runtime)
│   └── README.md                      # Instructions for downloading datasets
├── logs/                              # Runtime logs
│   └── selfcheck_log_*.log            # SelfCheckGPT run logs
│
├── result_collector.py                # Artifact collection pipeline (softmax, attention,
│                                      #   FC activations, contextual embeddings) via TriviaQA
├── data_reader.py                     # Dataset loading utilities for TriviaQA & FAVA-Annotation
├── model.py                           # Classifier architectures:
│                                      #   RNN, Single-layer MLP, Multi-layer MLP + Residual,
│                                      #   Multi-layer Transformer + Residual
├── eval_classifier.py                 # Train & evaluate classifiers; writes AUROC to score_result.txt
├── self_check_gpt.py                  # SelfCheckGPT baseline (bert-score & n-gram methods)
│
├── classifier_model.ipynb             # Interactive notebook for classifier experiments
├── extract_attn_maps.ipynb            # Notebook for extracting & visualising attention maps
├── hyperparam_tuning.ipynb            # Hyperparameter search notebook
├── self_check_gpt.ipynb               # SelfCheckGPT baseline notebook
│
├── score_result.txt                   # Saved AUROC / accuracy results
├── requirements.txt                   # Python dependencies
├── setup.sh                           # Environment setup script
└── .env.example                       # Template for HuggingFace token configuration
```

### File roles at a glance

| File | Purpose |
|---|---|
| `result_collector.py` | Runs inference on TriviaQA, collects model artifacts (softmax, IG attributions, attention weights, FC activations, contextual embeddings) and saves them as pickle files |
| `data_reader.py` | Loads and preprocesses TriviaQA and FAVA-Annotation datasets |
| `model.py` | Defines classifier architectures used for hallucination detection |
| `eval_classifier.py` | Trains classifiers on collected artifacts and reports AUROC / accuracy |
| `self_check_gpt.py` | Baseline: SelfCheckGPT with bert-score and n-gram consistency checks |
| `score_result.txt` | Persisted evaluation scores from `eval_classifier.py` |
